import 'dart:ffi';

import 'package:flutter/material.dart';

/// Default margins
const double kMarginXLG = 150.0;
const double kMarginLG = 80.0;
const double kMarginMD = 40.0;
const double kMarginSM = 20.0;
const double kMarginXS = 10.0;

/// Page content Margin
const EdgeInsets kContentMargin = EdgeInsets.fromLTRB(35, 0, 35, 0);

/// Typo
const TextStyle kInputText = TextStyle(fontSize: 22, color: Color(0xFF4A4A4A));
const TextStyle kInputPlaceholder = TextStyle(color: Color(0xFF8F8F8F));
const TextStyle kTextColor = TextStyle(color: Colors.white);
const Color kIconDark = Color(0xFF4A4A4A);

/// Buttons
const Color kButtonPrimaryColor = Color(0xFF1B99E2);

/// Input
const TextStyle kInputValidationError = TextStyle(
  color: Colors.white,
  backgroundColor: Colors.red,
);
